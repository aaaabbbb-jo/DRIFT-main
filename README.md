# DRIFT-main

You can download our R package using
```{r}
install.packages("devtools")
library(devtools)
install_github("aaaabbbb-jo/DRIFT-main")
```

The 'DRIFT Tutorial' shows how to apply the method
